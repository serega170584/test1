#doctrine:
    #dbal:
        # "TEST_TOKEN" is typically set by ParaTest
        # Временно закомментировал пока не будут решены проблемы с миграциями в тестовом окружении
        #dbname_suffix: '_test%env(default::TEST_TOKEN)%'
